import { TextSlicePipe } from './text-slice.pipe';

describe('TextSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new TextSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
